package ABBE

type abbeNode struct {
	left  *abbeNode
	right *abbeNode
	value *interface{}
}

func (self *abbeNode) insert(treeNode *abbeNode) {
	if (*self).left == nil && (*self).right == nil{
		if *((*self).value)
	}
}
